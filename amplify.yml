version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: ./dist # Force explicit path
    files:
      - '**/*'
# Triggering fresh build - Force sync
  cache:
    paths:
      - node_modules/**/*
